import {BrowserRouter, Switch, Route} from "react-router-dom";

const Router = () => {
    return <BrowserRouter>
        <Switch>
            <Route path="/">
            </Route>
        </Switch>
    </BrowserRouter>
}

export default Router;